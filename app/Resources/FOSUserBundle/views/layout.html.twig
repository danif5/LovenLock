{% extends 'FlorProjectFrontendBundle:Common:layout_general.html.twig' %}
{% block page_content %}
    <div style="padding-top: 60px; padding-bottom: 60px">
    {#<div>#}
    {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
    {#{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |#}
    {#<a href="{{ path('fos_user_security_logout') }}">#}
    {#{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}#}
    {#</a>#}
    {#{% else %}#}
    {#<a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>#}
    {#{% endif %}#}
    {#</div>#}

    <div class="container m-t-150">
        <div class="container text-03em p-t-40">
            {% for type, messages in app.session.flashBag.all %}
                {% for message in messages %}
                    <div class="alert alert-{{ type | replace({'fos_user_': ""}) }} text-center">
                        <span class="text-{{ type | replace({'fos_user_': ""}) }}">{{ message|trans({}, 'FOSUserBundle') }}</span>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        <div class="col-sm-8 col-sm-offset-2">
            {% block fos_user_content %}
            {% endblock fos_user_content %}
        </div>
    </div>
    <div class="clear-fix p-b-20"></div>
    </div>
{% endblock page_content %}
