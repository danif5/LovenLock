{% block form_errors -%}
    {% if errors|length > 0 -%}
        <ul class="bg-danger p-5">
            {%- for error in errors -%}
                <li class="text-danger">{{ error.message }}</li>
            {%- endfor -%}
        </ul>
    {%- endif %}
{%- endblock form_errors %}